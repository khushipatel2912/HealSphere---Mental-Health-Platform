const CrisisDescription = ({ title, description }) => (
    <div className="crisis-description">
        <h4>{title}</h4>
        <p>{description}</p>
    </div>
);

export default CrisisDescription;
